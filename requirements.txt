# Core
torch>=2.0.0
numpy>=1.24.0
tqdm>=4.65.0

# Config
pyyaml>=6.0

# NLP (future phases; not required for the minimal toy run)
transformers>=4.30.0
tokenizers>=0.13.0
sentencepiece>=0.1.99

# Data (future phases; not required for the minimal toy run)
datasets>=2.12.0
pyarrow>=12.0.0

# Training / logging (optional)
wandb>=0.15.0

# Dev
pytest>=7.3.0
black>=23.3.0
flake8>=6.0.0


